from setuptools import find_namespace_packages, find_packages, setup


setup(
    #packages=find_namespace_packages(include=['managers.*'], exclude=['sample.*']),
    #namespace_packages=['managers'],
)